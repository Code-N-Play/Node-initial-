1> download and install node
2> insatall express
3> insatall nodemon
4> set up  ejs
5> set up satic files ... html css js images etc. 
                    
                    (path important hai ejs mai jo likha jaega 
                    ejs main koi or file jo ki public ke ander stylr ke ander hai use tak pahuchane ka takira hota hai phle bahar nikalo current folder se uske bad public folder main jao but set line ke karan path main /public automaticly lag jata hai to isko mat laga na to path kuch is type ka dikhega ........   /stylesheet/style.css     phle liha hua path yetha ../public/stylesheet/style.css )



itna sab na krna pade is liye express generator use karo 
6> install Express generator
      npm i express-generator -g 
      go cmd 
      move to desktop (cd Desktop)
      fire the command ( express appname --view=ejs )
      cd appname
      npm i 
      code . ( to open file in vs code )
7> install mongoose 
    npm i mongoose 
8> how to connect database 
    go to user.js file inside the router folder
    require and setup 
    const mongoose = require ("moongoose");
    mongoose.connect = ("mongodb://localhost:27017/databaseName");
    
    
session and cookies

9> install session 
	npm i express-session
	
10> main script file me ye code add karo
	var session = require("express-session")
	app.use(session ({
		resave: false,
		saveUninitialized: false,
		secret : "lolopolop" 
	}));
	
11> create session === req.session.sessionname = sessionvalue;
12> read session === req.session.sessionname
13> delete session === req.session.destroy()


cookies 

already installed hai 

